# Minimal, Streamlit Cloud–friendly requirements
# Avoid native packages (faiss-cpu, ollama) in this file.

streamlit>=1.34.0
PyYAML>=6.0.1
python-dotenv>=1.0.1
requests>=2.31.0
pydantic>=2.7.0
tqdm>=4.66.0

# Document processing (pure-Python / pip-friendly)
pypdf>=4.2.0
python-docx>=1.1.2
sentence-transformers>=2.6.0

# Optional vector DB (use only if you intend to use Chroma in Cloud)
# chromadb>=0.5.0

# Testing / utils (optional)
# pytest>=8.2.0
